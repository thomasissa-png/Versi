# Lessons Learned — Versi

> Capitalisation des apprentissages actifs. Entrées anciennes (TTL > 5 sessions OU > 90 jours) dans [`lessons-learned-archive.md`](./lessons-learned-archive.md). Patterns devenus règles stables dans `CLAUDE.md` + agents concernés.

**Cap commandement n°8** : 80 L — net-zero par session. Toute nouvelle entrée nécessite la migration d'une ancienne vers archive OU la promotion en règle d'agent.

---

## Patterns actifs non-promus en règle (sessions s22-s25)

Sessions 2026-04-17 → 2026-04-23. Ces patterns attendent soit promotion, soit consolidation, soit expiration TTL au prochain audit.

| Session | P | Pattern | Statut | Action prochaine |
|---|---|---|---|---|
| s28 | 2026-05-03 | P0 | bug-fix-architectural | **`firstPlan = plans[0]` est un piège multi-étage** : dans une UI qui sélectionne un lot par étage, le code `const firstPlan = plans[0]` affiche TOUJOURS le 1er plan (RDC) quel que soit l'étage sélectionné. Sur Versi Studio Étape 3, ce bug a invalidé 26 tours d'audit visuel (les polygones R+1/R+2/R+3 étaient rendus sur le PDF du RDC). Détecté tour 27 par Thomas, fix : `plans.find(p => p.floor_number === selectedLot.floor_number)`. | Fix tour 27 commit `2549cbe` dans `rooms/page.tsx`. Pattern : **toute UI multi-plan/multi-étage doit filtrer dynamiquement par contexte sélectionné**, jamais utiliser `[0]` comme défaut. | brut | agent-spécifique | `.claude/agents/fullstack.md` (règle UI multi-context : pas de `[0]` comme PDF par défaut, toujours dériver du state actif) + `.claude/agents/qa.md` (test E2E par étage obligatoire pour UI multi-plan) | fait s28 | propagé s29 |
| s28 | 2026-05-03 | P0 | mock-obsolete-detection | **Mocks hardcodés non synchronisés avec données réelles = invisibilité du bug** : `plan-extractor-mock.ts` Versi Studio hardcodait des données T2/T3 (2 appartements) pour R+1, alors que le vrai PDF Muguets R+1 a 1 seul appartement avec 8 pièces. Causé 3 tours d'investigation orchestrator basés sur fausse interprétation visuelle. | À nettoyer (supprimer ou resync mock avec PDF réel). Pattern : **un mock ne doit JAMAIS hardcoder des structures absentes des données réelles** ; il doit utiliser le même pipeline que la vraie extraction, juste avec l'IA mockée (réponse fixe au lieu d'appel OpenAI). | brut | agent-spécifique | `.claude/agents/ia.md` (règle mocks synchronisés avec données réelles) + `.claude/agents/qa.md` (test comparaison mock vs réel obligatoire pour pipelines IA) | à-faire (cleanup mock file) | propagé s29 |
| s28 | 2026-05-03 | P0 | audit-visuel-orchestrator-strict | **Pattern « as-tu seulement vérifié ? » s'applique aussi à l'orchestrator** : 21+ tours d'audit visuel s28 ont été faux car comparaison superficielle PDF/screenshot. L'orchestrator doit lire le screenshot ET le PDF original côte à côte, vérifier bord par bord chaque polygone, AVANT tout claim de score. Auto-critique : « note 8/10 » donnée 4 fois sur des résultats qui valaient 4-5/10 visuel. | À propager : orchestrator role inclut désormais audit visuel rigoureux pre-claim. Si non vérifié → écrire « NON-VÉRIFIÉ — à valider par Thomas » plutôt que « validé X/10 ». | brut | règle-globale | `_base-agent-protocol.md` (section audit visuel orchestrator) + `.claude/agents/orchestrator.md` (gate pre-claim) | fait s29 | propagé s29 |
| s28 | 2026-05-03 | P1 | pivot-orthogonal-apres-plateau | **Après 5+ itérations sur la même approche algorithmique sans gain net, pivot architectural OBLIGATOIRE** : s28 a démontré que 7 itérations sur BFS+post-traitement plateau à 17/20, alors que pivot bbox-from-walls (tour 18) a fait progresser. Pivot suivants tour 29 (invariant touche), tour 32 (override cap PDF) ont chacun débloqué le plateau. Pattern existant `iterate-but-pivot-when-plateau` (s27.2) à renforcer. | Renforcer dans `_base-agent-protocol.md` : **gate explicite « après 5 tours sans gain net, l'agent DOIT proposer un pivot architectural orthogonal, pas insister sur la même approche »**. | brut | règle-globale | `_base-agent-protocol.md` (renforcer Règles s27.2 iterate-vs-pivot-honest avec gate 5-tours) | fait s29 | propagé s29 |
| s28 | 2026-05-03 | P1 | audit-mauvaises-raisons | **Audit qui passe pour mauvaises raisons = pire qu'un audit qui échoue** : tour 17 a claim 20/20 strict avec critère « k constant proportionnel » (cohérence interne des ratios) qui MASQUAIT que les surfaces absolues étaient fausses (ratio jusqu'à 12.8 vs PDF). Pattern : si l'audit passe mais le visuel est mauvais, l'audit mesure mal — il faut redéfinir le critère, pas valider. | À propager dans `@reviewer` : **vérifier que le critère d'audit reflète ce qui compte pour l'utilisateur final, pas juste un invariant mathématique interne**. Confronter audit numérique à audit visuel sur les premiers livrables avant industrialisation. | brut | agent-spécifique | `.claude/agents/reviewer.md` (gate audit visuel vs audit numérique) + `.claude/agents/qa.md` (idem) | fait s29 | propagé s29 |
| s29 | 2026-05-04 | P1 | timeout-brief-trop-dense | **API stream timeout sur briefs > 8 sections, même avec anti-timeout cmd n°3 explicite.** S29 : 4 agents ont timeouté en Phase 1+2 (PM 7m30 0 fichier, UX 7m24 sections 4-9 manquantes) sur briefs 9-12 sections. Cause racine : longue rédaction continue dépasse limites idle stream API. Fix empirique : skeleton-first explicite + max 5 reads + 1 Edit par section + 80L max par Edit + brief découpé en livrables séparés si > 7 sections. | Renforcer cmd n°3 anti-timeout : préciser "max 8 sections par livrable, sinon découper en 2 livrables séquentiels". Ajouter pattern "skeleton-first" en règle explicite plutôt que recommandation. | brut | règle-globale | `CLAUDE.md` (cmd n°3) + `_base-agent-protocol.md` (section anti-timeout) | fait s30 | propagé s30 |
| s29 | 2026-05-04 | P1 | persona-validation-arbitrage-fondateur | **Le persona testeur peut challenger un arbitrage fondateur déjà tranché — et avoir raison sur l'usage terrain réel.** S29 : Thomas a tranché "mobile + desktop obligatoire" en arbitrage C, mais @testeur-persona-thomas-marchand a flagué FAIL sur GP5 (placement tactile sur polygone 8m² impossible avec doigt qui couvre la pièce). Solution = "investissement spécifique placement tactile" (zoom auto + FAB + bottom sheet) plutôt que retour-arrière sur l'arbitrage. Pattern : **toujours intercaler une validation persona AVANT Phase 3 implémentation, même si les arbitrages produit sont tranchés**. Le persona valide l'usage, pas la stratégie. | À propager dans `@orchestrator` : insérer une étape "validation persona" obligatoire entre Phase 2 (specs+wireframes+pipeline) et Phase 3 (implémentation) sur les refontes structurelles. | brut | agent-spécifique | `.claude/agents/orchestrator.md` (workflow phasing : ajouter gate persona pre-implémentation) | fait s30 | propagé s30 |
| s29 | 2026-05-04 | P1 | doublon-emplacement-fichier | **Quand un projet a 2 dossiers similaires (`scripts/migrations/` ET `src/lib/vs/migrations/`), un agent va écrire dans les 2 par hésitation.** S29 : @fullstack a créé 7 migrations SQL au total (3 dans scripts/migrations/, 4 dans src/lib/vs/migrations/) parce que le brief n'a pas tranché explicitement. Cleanup orchestrator nécessaire post-livraison. Pattern : **dans le brief, toujours indiquer le dossier canonique exact pour chaque type de fichier** (ex : "Place les migrations dans `versi-studio/src/lib/vs/migrations/` — ce dossier est canonique, ignorer `scripts/migrations/`"). | À propager dans tous les briefs orchestrator code : section "Conventions chemin" obligatoire dans chaque brief @fullstack/@ia. | brut | agent-spécifique + prompts | `.claude/agents/orchestrator.md` (template brief code : section convention chemins) + `.claude/agents/fullstack.md` (rappel : si 2 emplacements possibles → demander avant écrire) | fait s30 | propagé s30 |
| s29 | 2026-05-04 | P0 | cost-reality-openai-vs-estimation | **Vérifier les tarifs OpenAI réels via WebSearch AVANT tout chiffrage de coût IA.** S29 : brief @ia mentionnait $0.04/image gpt-image-2 (basé sur estimation), réalité 2026 = $0.21/image (×5). @ia a vérifié WebSearch + ajusté coût projet à $4.25 (×4 vs $1 estimé). Pattern : **chaque agent IA qui chiffre un coût DOIT WebSearch les tarifs actuels du modèle invoqué et les citer**, pas se baser sur des estimations brief. | À propager dans `@ia` : règle obligatoire WebSearch tarifs avant chiffrage. | brut | agent-spécifique | `.claude/agents/ia.md` (règle "vérification tarifs OpenAI/Anthropic avant chiffrage") | fait s30 | propagé s30 |
| s29 | 2026-05-04 | P0 | pref-fondateur-credits-no-blocage | **[PRÉFÉRENCE FONDATEUR]** Thomas refuse les blocages techniques de budget. Verbatim s29 : *"Nonon pas de blocage d'argent c'est ok. Continue. Chaque visuel utilisera des crédits mais on verra plus tard cette partie."* Sur Étape 4 v2 : circuit breaker $5 proposé par persona → REJETÉ. Système de crédits utilisateur prévu V3 (pas de cap technique côté outil V2). Pattern : **proposer compteurs/affichages indicatifs OK, jamais de blocage technique sur dépense IA sans demande explicite**. | À ajouter dans `docs/founder-preferences.md` + propagation `@product-manager` (ne pas suggérer circuit breaker sauf demande explicite) + `@fullstack` (afficher coût indicatif jamais bloquer). | brut | founder-prefs + agent-spécifique | `docs/founder-preferences.md` (nouvelle préférence "pas de blocage budget IA") + `.claude/agents/product-manager.md` (règle features anti-friction budget) + `.claude/agents/fullstack.md` (règle UI : affichage coût ≠ blocage) | fait s30 | propagé s30 |
| s29 | 2026-05-04 | P0 | pref-fondateur-validation-persona-systematique | **[PRÉFÉRENCE FONDATEUR]** Thomas demande systématiquement validation persona AVANT implémentation sur refontes UX. Verbatim s29 : *"Pour la validation demande à marchand de bien persona de me donner son avis."* Pattern : **sur toute refonte structurelle ≥ 1 livrable spec, lancer obligatoirement testeur-persona avant Phase 3 implémentation**. Le persona challenge l'usage réel terrain. | À ajouter dans `docs/founder-preferences.md` + propagation `@orchestrator` (gate persona pre-implémentation systématique). | brut | founder-prefs + agent-spécifique | `docs/founder-preferences.md` + `.claude/agents/orchestrator.md` (workflow refonte : insérer gate persona) | fait s30 | propagé s30 |
| s30 | 2026-05-04 | P0 | sse-nextjs-replit-patterns | **SSE Next.js sur Replit/Vercel autoscale = 4 patterns OBLIGATOIRES** : (1) replay initial bdd avant subscribe bus, (2) keep-alive `: ka` 25s + `X-Accel-Buffering: no`, (3) heartbeat detect 60s côté client, (4) cleanup `request.signal.abort` + `useEffect return close()`. Validé `useVisualsStream.ts` + `visual-job-bus.ts` Étape 4 v2. | promu `.claude/agents/fullstack.md` Règles s30 (commit `7eee20c` à venir) | fait s30 | propagé s30 |
| s30 | 2026-05-04 | P0 | openai-toFile-mime-ext | **OpenAI Images API `toFile()` : extension fichier doit refléter le vrai MIME.** Bug s30 photos JPEG envoyées avec `photo.png` + `Content-Type: image/jpeg` → "Invalid image format" rejeté. Fix `b643629` : `ext` switch couvre tous MIME (jpg/png/webp). | promu `.claude/agents/fullstack.md` Règles s30 | fait s30 | propagé s30 |
| s30 | 2026-05-04 | P0 | tap-to-confirm-mobile | **Pattern tap-to-confirm pour interactions précises sur petits écrans** (fix P0 GP5 marchand de biens). Tap source + tap target (pas drag) + bottom sheet confirm + zoom auto pièce 70% mobile = doigt ne couvre jamais le polygone. Validé `PlacementBottomSheet.tsx` (commit `227b419`) + verdict persona GO 8.5/10. | promu `.claude/agents/ux.md` Règles s30 | fait s30 | propagé s30 |
| s30 | 2026-05-04 | P1 | vitest-vi-hoisted | **`vi.hoisted({...})` OBLIGATOIRE pour mocks Vitest partagés.** `const queryMock = vi.fn()` + référence dans `vi.mock()` factory cause `ReferenceError: Cannot access 'X' before initialization` (factory hoiste avant `const`). Pattern : `const { queryMock } = vi.hoisted(() => ({ queryMock: vi.fn() }))`. | promu `.claude/agents/qa.md` Règles s30 | fait s30 | propagé s30 |
| s30 | 2026-05-04 | P1 | playwright-cascade-fetch-mock | **Helper Playwright cascade fetch : exposer TOUTES les routes intermédiaires.** Page Next.js avec cascade (project → lots → plans → rooms → visuals) bloque sur skeleton si helper mock 1-niveau. Checklist : `grep "fetch(" page.tsx` AVANT rédaction tests E2E. | promu `.claude/agents/qa.md` Règles s30 | fait s30 | propagé s30 |
| s30 | 2026-05-04 | P2 | doc-gap-post-livraison | **Pattern gap documentaire post-livraison.** Pointeurs "à venir/TBD/next phase" doivent être mis à jour quand le livrable cible est commité (ex : `REPLIT_ACTIONS.md §6 "Vague 3 à venir"` obsolète après commits 227b419/cff35e1). Détecté par @reviewer audit final s30 — proposition gate G33. | brut | `.claude/agents/reviewer.md` Règles s31 (à propager si récurrent) | à-faire | non-propagé |
| s30 | 2026-05-04 | P1 | helper-first-react-testabilite | **Helper-first pour testabilité React sans jsdom.** Round 2 perfection : extraire la logique pure des composants React (calculs `useMemo`, transformations data, dérivations) en helpers `src/lib/[domain]/ui/[helper].ts` AVANT de tester. Helper porté par tests, composant = coquille de rendu. Évite install jsdom + @testing-library/react (~10 MB deps + setup vitest config). 18 tests s30 round 2 PASS en env Vitest `node`. Économie ~2h setup + 0 dette deps. | promu `.claude/agents/fullstack.md` Règles s31 | fait s30 | propagé s30 |
| s30 | 2026-05-04 | P1 | tests-source-level-vitest-node | **Tests source-level pour fixes JSX code-only en env Vitest `node`.** Round 3 perfection : pattern `readFileSync(component.tsx) + assertions regex/contains` sur le code lui-même (présence classe Tailwind, prop, var CSS, handler). 22/22 tests round 3 PASS en 8 ms. Avantages : zéro setup jsdom, valide invariants structurels post-fix. Limites : ne valide pas rendu visuel ni runtime — combiner avec Playwright E2E. Pattern réutilisable pour tout audit fix code-only. | promu `.claude/agents/qa.md` Règles s31 | fait s30 | propagé s30 |
| s31 | 2026-05-05 | P0 | tests-verts-pas-livraison | **La couverture de test ne prouve PAS que la feature est sur le chemin utilisateur.** S30 : nouvelle UI Étape 4 v2 codée+testée (Vitest 107/107, Playwright 18/0/2 PASS, verdict persona Thomas 8.5/10) MAIS la route active `/visuals` rendait toujours `VisualRoom` v1 → Thomas voyait l'ancienne UI + job synchrone bloqué 10 min. Tests greens sur `/visuals/placement` (la nouvelle UI), zéro test sur `/visuals` (la route active). Aucun grep/check post-livraison "la route active rend bien la nouvelle UI ?". Fix HOTFIX-2 (commit s31) : `visuals/page.tsx` = Server Component `redirect()` vers `/placement`. Pattern à propager : **avant de claim "feature livrée", grep la route active utilisateur + vérifier qu'elle importe les composants de la nouvelle UI**, pas seulement que les tests passent. Gate G34 candidat. | Propagé s31 : `.claude/agents/qa.md` Règles s31 (gate "wire-grep route active" pre-claim) + `.claude/agents/fullstack.md` Règles s31 (étendre wire-grep s27.2 aux routes UI) + `.claude/agents/reviewer.md` Règles s31 (audit "route active = nouvelle UI ?" obligatoire) | propagé | propagé s31 |
| s31 | 2026-05-05 | P1 | cleanup-v1-pre-merge | **Avant merge HOTFIX UI v2 → main, cleanup TOUS les composants v1 orphelins + déprécier routes legacy fire-and-forget.** Sinon dette technique masquée par les tests sur la nouvelle UI. S31 : après HOTFIX-2 redirect `/visuals` → `/visuals/placement`, 3 composants v1 (`VisualRoom`, `RoomGrid`, `VisualResult`) restaient orphelins zéro import + 1 route legacy `POST /api/vs/rooms/[id]/generate` en pattern fire-and-forget (`void runCoherentMonoRoom`). Commit `f51a33b` : suppression composants + route → 410 Gone. Élimine GP3 PARTIAL verdict persona Thomas (8.5/10 → 10/10 cible). Pattern : checklist post-HOTFIX UI = (1) `grep -rn "import.*OldComponent" src/ tests/` confirme 0 import → supprimer fichiers, (2) `grep -rn "void.*runCoherent\|fire-and-forget" src/` détecte routes legacy → migrer ou déprécier en 410. | À propager `.claude/agents/fullstack.md` Règles s31 (checklist post-HOTFIX UI : audit orphelins + audit fire-and-forget legacy) + `.claude/agents/reviewer.md` Règles s31 (audit cleanup obligatoire post-HOTFIX UI v2). | brut | agent-spécifique | post-propagation s32 | fait s31 | non-propagé |
| s26 | P0 | **Sandbox Claude Code = "DNS cache overflow" 18 B 503** sur domaines non-whitelistés (google, drive, leboncoin, microsoft, ...). Confondu 6 fois cette session avec de vrais bugs prod (versi.fr/versi-immobilier/Drive). Toujours **distinguer** via body curl : 18 B `text/plain` "DNS cache overflow" = sandbox bloque, sinon vrai 503. | propagé s27 | `_base-agent-protocol.md` section "Règles s26" |
| s26 | P0 | **Compléter ≠ remplacer** sur édition copy demandée. Quand Thomas dit "ajouter 3 mots Cashflow/TRI/CoC", garder TOUTE la phrase d'origine + ajouter les 3 mots. Réécrire = violation. | propagé s27 | `_base-agent-protocol.md` section "Règles s26" + `docs/founder-preferences.md` ligne 244-247 |
| s26 | P0 | **Reality check VISUEL pixel-par-pixel** (cmd n°7) violé 3 fois en s26 : (a) bloc metrics homepage versi-invest pondéré 80 mots vs autres steps 15 mots — pas vu avant push, (b) hero friedland-2eme-droite "Séjour après rénovation 2.JPG" = chambre vide parquet brut → catastrophe selon Thomas, (c) ProcessPage step 03 idem. À chaque fois corrigé après plainte Thomas. | propagé s27 | `_base-agent-protocol.md` section "Règles s26" — gate `@reviewer` + `@design` à finaliser |
| s26 | P1 | **Pattern audit visuel @moi via Read multimodal direct** : copier photos vers `/tmp/<slug>.jpg` ASCII-only depuis sandbox NFC-bug, puis Read pour notation /10. Évite le bug NFD/NFC du Read tool sur sub-agents. Validé sur 9 photos audit P1+P2. | propagé s27 | `_base-agent-protocol.md` section "Règles s26" — finaliser dans `@moi` |
| s26 | P1 | **Architecture photos durable = pré-compilation locale + commit JPEG dans repo + sharp en devDep**. Pattern `generate-photos.js` (sharp 1600px q85) → `manifest.json` source de vérité → autoSeed prod ne fait QUE INSERT URLs (~1s). Solution au timeout Neon 57P01 (autoSeed > 60s avec resize au boot). | propagé s27 | `docs/founder-preferences.md` ligne 249-252 (solution propre durable) — finaliser dans `@fullstack` + `@infrastructure` |
| s26 | P1 | **Net-zero cmd n°8 enforced via archive-first** : avant trim CLAUDE.md/lessons-learned/project-context, créer fichier archive et copier le contenu, puis trim. Zéro perte garantie même si tool échoue mid-process. Validé sur 3 fichiers (1209 L archivées s26). | propagé s27 | `_base-agent-protocol.md` section "Règles s26" |
| s26 | P2 | **Anti-doublon photos partagées** : 2 projets utilisant le même dossier source (Apparts Jardin = 1er+2ème étage Prieuré) doivent avoir des `apresFiles` + `avantFiles` EXPLICITES disjoints. Sinon le même hero apparaît sur 2 cartes voisines = casse la promesse "biens distincts". Vérifié post-curation : 0 doublon entre les paires. | brut | `@creative-strategy` workflow curation |
| s26 | P0 | **[PRÉFÉRENCE FONDATEUR]** Thomas REFUSE les rallonges textuelles non demandées. Quand il dit "rajouter X" il veut X et SEULEMENT X. Toute amélioration "pour bien expliquer" est rejetée comme "bloc moche pas revu". Ton minimaliste, factuel, identique au pattern existant. | propagé s27 | `docs/founder-preferences.md` (lignes 244-247) — propagation `@copywriter` à finaliser |
| s26 | P0 | **[PRÉFÉRENCE FONDATEUR]** Thomas exige solution **propre et durable**, JAMAIS quick fix. Quand on lui propose "lazy load + resize quick win", il répond "à l'équipe de travailler l'architecture, pas à moi. Solution propre et durable". → Refuser tout patch cosmétique, designer la vraie architecture (cf. migration photos s26). | propagé s27 | `docs/founder-preferences.md` (lignes 249-252) — propagation `@infrastructure` + `@fullstack` à finaliser |
| s26 | P1 | **[PRÉFÉRENCE FONDATEUR]** Thomas demande des **chiffres ronds** pour la communication publique : `660 639 € → 660 000 €` sur emprunt Arras. Exception assumée pour les chiffres exacts PDF (frais notaire 60 639 € = chiffre acte officiel). Préférer la lisibilité commerciale à la précision absolue sur les sommes en cents. | propagé s27 | `docs/founder-preferences.md` (lignes 254-256) — propagation `@copywriter` à finaliser |
| s26 | P1 | **[PRÉFÉRENCE FONDATEUR]** Thomas exige **anonymisation des adresses** sur les fiches publiques refs : "rue d'Arras à Lille" → "à Lille (59)". Sécurité + protection portfolio. Mais nom de SCI (MMM/MMO/MLV) acceptable car déjà sur le PDF descriptif. | propagé s27 | `docs/founder-preferences.md` (lignes 258-261) — propagation `@copywriter` à finaliser |
| s26 | P1 | **[PRÉFÉRENCE FONDATEUR]** Thomas considère que **le hero d'une fiche immobilière = la photo qui fait cliquer**. Espace de vie meublé > vue détail > photo vide. Une chambre vide en parquet brut comme hero = "casse la promesse marchand de biens livre du fini". Skip les photos médiocres même si on tombe à 2 photos après seulement (qualité > quantité). | propagé s27 | `docs/founder-preferences.md` (lignes 263-266) — propagation `@design` + `@creative-strategy` à finaliser |
| s27 | P0 | **[PRÉFÉRENCE FONDATEUR]** Thomas exige `gpt-image-2` (PAS `gpt-image-1`) sur tout appel `openai.images.*`. Préférence ratée 2 fois (s25 + s27) malgré mémo s26 explicite. Verbatim s27 : *"Merci d'utiliser gpt-image-2 et pas 1 !! J'ai déjà dit ça."* Investigation gpt-image-2 défaillant = niveau prompt/hyperparamètres/org KYC, JAMAIS fallback sur gpt-image-1. | propagé s27 | `docs/founder-preferences.md` ligne 242-243 + `_base-agent-protocol.md` règle préchecklist commit IA |
| s27 | P0 | **[PRÉFÉRENCE FONDATEUR]** Thomas refuse les fallbacks automatiques sur tâches IA critiques. Verbatim : *"Je ne veux pas de fallback. Je veux que ça marche bien c'est tout."* Stratégie "si X échoue → bascule auto sur Y dégradé" rejetée — préfère échec visible + investigation. Le filet de sécurité masque le vrai problème, l'investigation est faite à la source. | propagé s27 | `docs/founder-preferences.md` ligne 245-247 + `_base-agent-protocol.md` règle pattern fallback |
| s27.2 | 2026-04-30 | P0 | technique-adjacente | **Bitmap a un plafond mathématique pour pixel-perfect** : 9 itérations bitmap (Moore boundary trace + snap-to-wall + autopilot grid 24-108 configs + dense filter + orthogonalize) = plateau IoU 0.80 sur 4 plans Muguets. La rasterisation @scale=3 sur 9M pixels introduit du bruit que ni filtres ni morphologie ne peuvent éliminer. Quand Thomas demande "EXACTEMENT" sur du parsing géométrique, **toujours envisager le PDF source comme vérité unique avant de tuner** : commit `670efba` pivot vectoriel a fait IoU plateau → polygon 4 sommets pixel-perfect en 1 commit. | Validé empiriquement s27 sur 4 plans. Pattern à promouvoir : avant 5+ itérations algo → step back et chercher la source de vérité une couche au-dessus. | brut | règle-globale + agent-spécifique | `.claude/agents/ia.md` (Règles s27.2 step-back-source-truth) + `_base-agent-protocol.md` (Règles s27.2 iterate-vs-pivot-honest) — CLAUDE.md non-modifié pour respecter cap 125 (couverture équivalente via base-agent-protocol + ia) | fait s28 | propagé s28 |
| s27.2 | 2026-04-30 | P0 | viewport-transform-pdfjs | **pdfjs-dist `getOperatorList()` ne pré-applique PAS `viewport.transform` aux paths**. 3 fois raté en début de pivot vectoriel (« coords hors page », bbox négatif). Probe runtime obligatoire AVANT tout parsing : `console.log(viewport.transform)` puis utiliser comme CTM initial dans le walk operatorList. Fix : `let ctm = viewport.transform; ctm = multiplyCtm(ctm, [a,b,c,d,e,f])` à chaque OPS.transform. | Documenté dans `lot-vector-extractor.ts` (commentaire ligne 165-175). Pattern à propager pour tout futur parsing PDF vectoriel. | brut | agent-spécifique | `.claude/agents/ia.md` (pattern probe-runtime-pdf-parser) | fait s28 | propagé s28 |
| s27.2 | 2026-04-30 | P0 | wire-prod-vs-diagnostic | **Modifier la route diagnostic ne suffit pas — toujours grep+wire la route prod.** Bug Thomas « j'ai bien mis à jour et pourtant j'ai la même erreur, comme si pas mis à jour » → root cause : route `/api/vs/diagnostics/pipeline-new` modifiée mais route prod `/api/vs/projects/[id]/extract` utilisait encore l'ancien pipeline. Pattern : avant de fermer un changement « pipeline », `grep -rn "OldFunction\|NewFunction" src/app/api` pour confirmer que TOUS les call-sites prod sont migrés. | À ajouter dans `_base-agent-protocol.md` checklist pre-commit pipeline | brut | règle-globale | `_base-agent-protocol.md` (section pre-commit pipeline) + `.claude/agents/fullstack.md` (pattern wire-grep-confirm) | fait s28 | propagé s28 |
| s27.2 | 2026-04-30 | P1 | drag-undo-pattern | **Drag UX = 1 seul undo entry**. Pattern standard apps de dessin (Figma, Sketch, Excalidraw) : pendant le drag, mise à jour visuelle SANS snapshot historique ; au mouseup, UN snapshot final. Implémentation : 2 callbacks séparés (`onZoneChange` visuel + `onZoneCommit` historique). Bug avant fix : Ctrl+Z décomposait le déplacement en N micro-undos par pixel. | Pattern à propager dans `@ux` + `@fullstack` (canvas / drag UX) | brut | agent-spécifique | `.claude/agents/ux.md` (pattern drag-commit-séparé) + `.claude/agents/fullstack.md` (pattern dual-callback-canvas) | fait s28 | propagé s28 |
| s27.2 | 2026-04-30 | P1 | preselect-on-load | **Présélection au chargement = visibilité par défaut**. Quand Thomas dit « le lot n'est pas assez visible de base », fix le plus efficace = sélectionner automatiquement le 1er élément au load (pas changer le styling de l'élément non-sélectionné). Pattern useEffect + ref pour ne pas réécraser une sélection user. | À propager dans `@ux` (pattern feedback visuel par défaut) | brut | agent-spécifique | `.claude/agents/ux.md` (pattern preselect-default-visibility) | fait s28 | propagé s28 |
| s27.2 | 2026-04-30 | P0 | copy-pattern-unified | **[PRÉFÉRENCE FONDATEUR] Pour boutons UI similaires, pattern unifié `Action principale (précision)` > formulations marketing « jolies »**. Verbatim Thomas s27 : *« francembt c'est nul. Pourquoi ne pas faire quelque chose de clair type Ajouter un lot (forme rectangulaire) / Ajouter un lot (forme libre) ? »*. @copywriter avait recommandé « Lot rapide / Lot sur mesure » (vocab marketing immo) — rejeté car ne dit pas EXPLICITEMENT qu'on ajoute un lot ni la différence concrète. Pattern unifié action+précision = clarté maximum, action principale identique, différenciation lisible entre parenthèses. | À propager dans `@copywriter` (pattern UI buttons unified) + `docs/founder-preferences.md` | brut | agent-spécifique + founder-prefs | `.claude/agents/copywriter.md` (règle UI buttons : verbe d'action + précision parenthèses pour différenciation, pas formulations marketing) + `docs/founder-preferences.md` (nouvelle préférence) | fait s28 | propagé s28 |
| s27.2 | 2026-04-30 | P1 | autopilot-grid-time-limit | **Autopilot grid params = time-box strict 5 min sinon kill et réduire grille**. Tentative grid 108 configs = 36+ min projeté (~30s/config × 4 plans). Killed après 4 itérations en 5 min. Réduit à 24 configs → toujours 12 min, killed à nouveau. Pattern : commencer par 12-18 configs ciblées (paramètres clés × 2-3 valeurs chacun), évaluer manuellement le best, refiner si gain marginal. | À propager dans `@ia` + `@qa` | brut | agent-spécifique | `.claude/agents/ia.md` + `.claude/agents/qa.md` (règle autopilot grid time-box) | fait s28 | propagé s28 |
| s27.2 | 2026-04-30 | P0 | iterate-vs-pivot-honest | **[PRÉFÉRENCE FONDATEUR] Thomas exige itération autopilote MAIS pivot honnête quand plafond atteint**. Verbatim 13× : *« itère en autopilote jusqu'au résultat parfait ne t'arrête pas »*. Mais aussi : *« encore une fois je veux juste que tu testes toutes les approches jusqu'a trouver une qui marche, et me presenter le resultat ifnal. Les details ne minteressent pas »*. Lecture combinée : itérer SANS s'arrêter MAIS être honnête sur les plafonds (« bitmap plafonné à 0.80, je pivote vector »). Ne pas faire semblant que la 10e itération bitmap va finir par marcher. | À propager dans `_base-agent-protocol.md` (section persistence vs honesty) + `docs/founder-preferences.md` | brut | règle-globale + founder-prefs | `_base-agent-protocol.md` + `docs/founder-preferences.md` (nouvelle préférence "iterate but pivot when plateau") | fait s28 | propagé s28 |
| s27.2 | 2026-04-30 | P1 | verifier-rigoureusement | **[PRÉFÉRENCE FONDATEUR] « As-tu seulement vérifié ? »** = trigger d'auto-critique honnête. Quand on déclare « RÉSULTAT FINAL » sans avoir comparé bord par bord avec la référence, Thomas le détecte. Pattern : avant tout claim « parfait/final », vérifier rigoureusement (zoom sur chaque bord du polygon vs mur référence) ET formuler l'auto-critique honnête (« RDC bord haut OK / R+1 bord droit déborde 30px / R+2 ... »). | À propager dans `_base-agent-protocol.md` (gate verification rigoureuse pre-claim) + agents @reviewer + @qa | brut | règle-globale + agent-spécifique | `_base-agent-protocol.md` + `.claude/agents/reviewer.md` + `.claude/agents/qa.md` | fait s28 | propagé s28 |

---

## Format d'ajout d'un learning

Ajouter une ligne dans le tableau ci-dessus uniquement si :
1. Le learning n'est pas déjà couvert par un commandement CLAUDE.md ou un agent
2. Une entrée plus ancienne est archivée en contrepartie (net-zero)
3. Le statut est suivi : `brut` → `propagé` → `stable` → `archive` (TTL)

Voir `project-context.md` section "Historique des interventions agents" pour le journal complet.

# Audit @creative-strategy — Etape 0 Dashboard

| # | Critere | Note /10 | Justification | Correction exacte (si < 10) |
|---|---|---|---|---|
| C1 | Coherence palette | 7 | Les tokens semantiques (`bg-bg-card`, `border-border-default`, `text-text-muted`, `interactive-primary`) semblent correctement brandes. Aucune couleur accent parasite codee en dur. Probleme : `bg-error/10` et `text-error` introduisent un rouge fonctionnel qui n'est pas documente comme faisant partie de la palette Versi — acceptable techniquement mais a verifier dans le design system. Le spinner `border-t-interactive-primary` est propre. Pas de bleu, pas de vert startup. | Verifier que `error` dans le design system est bien le charcoal-error Versi et non un rouge generique Bootstrap. Si rouge : remplacer par un ambre/ocre compatible stone. `border-t-interactive-primary` sur le spinner : confirmer que `interactive-primary` = charcoal (et non un bleu residuel). |
| C2 | Hierarchie visuelle | 6 | Le H1 "Mes operations" est classe `vs-h3` — c'est un H1 semantique avec le style d'un H3 visuel. Probleme de signal : sur une page vide, le titre est petit, le bouton "Nouvelle operation" prend autant de poids visuel que le titre. L'etat vide (icone batiment + texte) est centre mais l'icone est generique (batiment SVG path). La grille de projets (`grid gap-md`) n'a pas de logique de colonne documentee. Pas de mur de texte, mais la hierarchie titre > action n'est pas nette : le CTA est a droite du titre sans poids visuel superieur. | L. 76 : passer `vs-h3` en `vs-h1` ou `vs-h2` pour que le titre porte son rang semantique. L. 77-88 : augmenter la taille du bouton CTA (`px-xl py-md text-base`) pour qu'il soit l'action primaire evidente, pas un bouton secondaire en taille. |
| C3 | Ton premium | 5 | "Aucune operation. Creez-en une pour commencer." — ce texte est celui d'un outil Notion ou d'une todo-list grand public. Pour un marchand de biens qui gere des immeubles de plusieurs millions d'euros, le registre est trop familier et trop generique. Le titre "Mes operations" est correct (vocabulaire metier). Le placeholder "12 rue de la Republique, 69001 Lyon" est un bon signal de specificite. Mais le formulaire titre "Nouvelle operation" sans sous-texte contextuel, les labels "Adresse" / "Type de bien" / "Surface totale (m2, optionnel)" sont fonctionnels mais sans personnalite. `rounded-lg` sur la carte formulaire est arrondi SaaS, pas premium institutionnel. | L. 141-143 : remplacer "Aucune operation. Creez-en une pour commencer." par "Aucune operation en cours. Lancez votre premiere operation immobiliere." ou simplement "Aucune operation active." (sec, factuel, pro). L. 214 : `rounded-lg` → `rounded-sm` ou `rounded` pour un rendu plus rigoureux. L. 216 : ajouter un sous-texte discret sous "Nouvelle operation" : `<p class="text-sm text-text-muted mt-xs">Decoupage de lots, plans, metrages</p>` pour contextualiser sans surevendre. |
| C4 | Alignement persona Thomas | 6 | Points positifs : vocabulaire "operation" (pas "projet" ou "dossier"), STATUS_LABELS metier (Plans uploades, Lots decoupe, Pieces identifiees), champ surface en m2, type de bien "immeuble" par defaut. Points faibles : l'etat vide avec une icone de batiment generique et un texte invite sont de la pedagogie grand public. Thomas sait ce qu'est une operation — il n'a pas besoin d'etre guide vers le premier clic. La page n'affiche aucune metrique de synthese (nb d'operations actives, surface totale geree) qui parlerait immediatement a un pro. Le formulaire demande adresse + type + surface mais pas de reference interne / nom de code, information critique pour un pro qui gere plusieurs operations en parallele. | L. 75-89 : ajouter une ligne de stats au-dessus du CTA si projects.length > 0 : `X operations — Y m² geres`. Montre immediatement la valeur et parle au pro. Formulaire L. 224+ : ajouter un champ "Reference / nom court" (ex: "Immeuble Bellecour") pour que Thomas retrouve ses operations sans lire les adresses completes. |
| C5 | Differenciateur visible | 4 | Rien dans cette page ne dit "c'est Versi". Pas de logotype, pas de signature editoriale, pas de traitement typographique distinctif. L'icone batiment est un SVG heroicons standard. La grille de projets est une liste de cartes comme dans Linear, Notion, ou tout autre SaaS. Le nom "Versi Studio" n'apparait pas. Pas de tonalite propre : ni un manifeste, ni une accroche, ni un detail visuel qui ancre l'identite. Meme le wording "operations" (bon signe) n'est pas capitalise ou presente comme un vocabulaire proprietaire. La page est une coquille fonctionnelle sans signature de marque. | Ajouter en haut de page, sous le header global, un chapeau de 1 ligne contextualisante : "Versi Studio — Decoupage et metrages de vos operations." ou juste le nom de l'outil en `vs-h4` avec `text-text-muted`. Remplacer l'icone SVG heroicons par une icone SVG custom ou au minimum un traitement typographique (lettre V stylisee, carre charcoal). L'etat vide est le moment de marque le plus fort — c'est la premiere chose que Thomas voit. Actuellement il ne dit rien de Versi. |

**Score global** : 5,6/10 (arrondi 6/10)

**Corrections prioritaires** :
1. `page.tsx:141-143` — Remplacer le texte etat vide par un registre pro factuel : "Aucune operation active." + sous-texte optionnel "Lancez votre premiere operation pour commencer le decoupage."
2. `page.tsx:76` — Passer `vs-h3` en `vs-h1` ou `vs-h2` — le titre principal de page ne peut pas porter le style d'un sous-titre
3. `page.tsx:74-89` — Ajouter un bloc identitaire minimal au-dessus du header : le nom "Versi Studio" et un sous-titre fonctionnel (1 ligne). Actuellement zero ancrage de marque.
4. Formulaire `page.tsx:224+` — Ajouter un champ "Nom de code / reference" avant l'adresse — premier besoin d'un pro qui gere plusieurs operations.
5. `page.tsx:79-88` — Augmenter le poids visuel du CTA principal pour qu'il soit l'action primaire evidente, pas un bouton de taille secondaire.

**Handoff** : score 6/10. Top 3 problemes : (1) zero differenciateur Versi — page anonyme et generique, (2) ton etat-vide grand public incompatible avec le profil Thomas, (3) hierarchie visuelle H1 declasse en vs-h3 et CTA sous-dimensionne. Verdict : fonctionnel mais pas premium, pas Versi. Corrections rapides (wording + 1 champ + chapeau de marque) suffisent a monter a 8/10 sans redesign.

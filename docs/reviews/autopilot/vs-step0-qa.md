# Audit @qa — Etape 0 Dashboard

| # | Critere | Note /10 | Justification | Correction exacte (si < 10) |
|---|---|---|---|---|
| C1 | 0 bug visible | 8/10 | Pas de warning React evident, keys correctes sur .map, AbortController propre (ligne 54 : `if ((err as Error).name === "AbortError") return`), cleanup effet (ligne 64). BUG MINEUR : ligne 57 `if (!signal?.aborted) setLoading(false)` — sur retry via `fetchProjects()` sans signal, `signal?.aborted` est undefined (falsy), donc `!undefined = true`, donc setLoading(false) s'execute correctement. OK mais logique confuse. BUG REEL : si erreur ET signal abort simultane, setLoading reste true (l'erreur est bien ignoree mais le spinner reste). Cas rare mais possible. | page.tsx:56-58 — Restructurer le finally : `if (signal?.aborted) return; setLoading(false);` au debut du finally, avant tout autre code pour eviter l'etat intermediaire confus. |
| C2 | Etats UI 5/5 | 9/10 | default (cartes lignes 148-154) : OK. loading (lignes 102-107) : spinner + texte OK. vide (lignes 123-145) : icone + message OK. erreur (lignes 110-120) : bandeau + retry OK. succes : implicite via redirection (`router.push` ligne 69) — pas de toast "Operation creee". Pas de feedback visuel de succes apres creation, juste une redirection silencieuse. | page.tsx:67-70 — Ajouter un toast de succes avant redirection, ex: `toast.success('Operation creee')` ou passer `?created=1` dans l'URL de destination pour afficher une confirmation sur la page suivante. |
| C3 | Validation inputs | 6/10 | Adresse : validee cote client (min 5 chars ligne 178, `required` + `minLength={5}` ligne 239-240) OK. Type_bien : select ferme, pas d'injection possible OK. Surface_totale : `type="number"` + `min={1}` (ligne 292), MAIS aucune limite max (Thomas peut taper 9999999), aucune validation cote client explicite, `parseInt(surfaceTotale, 10)` ligne 188 peut produire NaN si valeur exotique (ex: "1e10" → 10000000000). Pas de validation step (entier attendu mais `step` non specifie — accepte les decimaux sur certains navigateurs). Pas de trim de l'adresse avant verification `< 5` (ligne 178) : `adresse.trim().length < 5` est fait, OK. Mais pas de max length (SQL injection peu probable mais DoS possible avec 100KB d'adresse). Adresse avec emojis/accents : non testee. | page.tsx:174-209 — (1) Ajouter `maxLength={200}` sur input adresse. (2) Ajouter `max={100000}` et `step={1}` sur input surface. (3) Valider surface : `if (surfaceTotale && (isNaN(parseInt(surfaceTotale, 10)) \|\| parseInt(surfaceTotale, 10) < 1 \|\| parseInt(surfaceTotale, 10) > 100000))`. (4) Pas d'espace en double dans l'adresse : `adresse.trim().replace(/\s+/g, ' ')`. |
| C4 | Feedback actions | 8/10 | Submit : bouton disabled (ligne 308) + texte "Creation..." (ligne 318) OK. Bouton liste : spinner global OK. Retry erreur : bouton "Reessayer" OK. MAIS : pas de feedback de succes apres creation (voir C2), pas de protection contre double-submit rapide (si l'utilisateur double-clique tres vite avant que `setSubmitting(true)` ne s'applique — React batch normalement mais edge case possible). Pas de focus management : apres ouverture du formulaire (ligne 92), le focus n'est pas transfere sur le premier champ — probleme accessibilite + UX (utilisateur clavier doit re-Tab). | page.tsx:92-99 — Utiliser un `ref` avec `useEffect` sur `showForm` pour focus auto sur input adresse. Ex: `useEffect(() => { if (showForm) adresseRef.current?.focus() }, [showForm])`. |
| C5 | Erreurs gerees | 7/10 | AbortController propre (ligne 62-64). Erreur fetch GET geree (ligne 53-55). Erreur POST geree (ligne 204-205). MAIS : (1) Aucune distinction entre 500, 503, 404, 401 — tous deviennent "Impossible de charger les operations". Thomas ne sait pas si c'est une panne (503) ou un probleme permanent (404 = endpoint absent). (2) Pas de retry automatique sur 503 (on recommandait un retry backoff pour les DB 503 mentionnes dans le contexte). (3) `res.ok` non verifie — si le serveur renvoie 500 avec un JSON non-conforme, `await res.json()` peut throw avant le `if (json.success)`. Capture par le `catch` mais message generique. (4) Pas de timeout fetch (si le serveur repond en 60s, l'utilisateur voit un spinner infini). | page.tsx:41-59 — (1) Ajouter un AbortController avec timeout : `setTimeout(() => controller.abort(), 10000)` pour un timeout 10s. (2) Verifier `if (!res.ok)` et lire `res.status` pour message contextuel (503 → "Service temporairement indisponible, reessayez dans quelques secondes"). (3) Retry auto sur 503 avec 1 tentative apres 2s. (4) Meme traitement sur POST (ligne 191-205). |

**Score global** : 7.6/10

**Corrections @fullstack** (score < 10) :
1. **page.tsx:41-59** — Gestion d'erreurs HTTP differenciee : verifier `res.ok`, lire `res.status`, messages contextuels par code (503 vs 404 vs 500), ajouter timeout fetch 10s via AbortController, retry auto 1x sur 503.
2. **page.tsx:174-209** — Validation stricte du champ surface_totale : `max={100000}`, `step={1}`, verification `isNaN` et bornes explicites avant envoi. Ajouter `maxLength={200}` sur adresse.
3. **page.tsx:67-70** — Feedback de succes apres creation : toast ou parametre URL `?created=1` pour confirmation visible sur la page suivante.
4. **page.tsx:92-99** — Focus management : focus automatique sur le premier champ (adresse) a l'ouverture du formulaire via `useRef` + `useEffect([showForm])`.
5. **page.tsx:56-58** — Restructurer le `finally` de fetchProjects : sortir tot si `signal?.aborted` pour clarifier la logique et eviter l'etat intermediaire spinner + erreur.
6. **page.tsx:191-205** — Meme traitement d'erreur HTTP que GET : verifier `res.ok`, messages contextuels, timeout fetch.

**Handoff → @orchestrator**
- Score : **7.6/10** (C3 et C5 en dessous du seuil 8)
- Top 3 corrections : (1) gestion erreurs HTTP differenciee + timeout fetch, (2) validation stricte surface_totale + maxLength adresse, (3) feedback de succes post-creation (toast ou URL param)
- Verdict : **NO-GO strict** pour Etape 1 — C3 (validation) a 6/10 est critique : un formulaire sans limite max sur les nombres est une faille d'integrite donnees. C5 (erreurs) a 7/10 est acceptable mais le manque de retry 503 contredit les "fixes recents" du contexte. Corriger C3 et C5 avant Etape 1. C1/C2/C4 sont acceptables en l'etat.

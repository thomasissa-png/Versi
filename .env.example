# SMTP Configuration (identique pour tous les sites Versi)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=contact@versi.fr
SMTP_PASS=votre_mot_de_passe_smtp

# Optionnel — override du destinataire
# CONTACT_EMAIL=contact@versi.fr

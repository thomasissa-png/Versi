// Endpoints API internes — formulaires envoyés via SMTP (pas de service tiers)
export const CONTACT_ENDPOINT = '/api/contact';
export const SELL_ENDPOINT = '/api/sell';

export const CONTACT_EMAIL = 'contact@versi.fr';
